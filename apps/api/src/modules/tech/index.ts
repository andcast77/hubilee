/**
 * Tech Module
 *
 * Handles: technical assets, work orders, parts, service visits.
 * Requires tech module access via tenant context.
 */
export { registerRoutes } from '../../controllers/v1/tech.controller.js'
